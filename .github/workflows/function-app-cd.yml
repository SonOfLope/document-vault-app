name: DocumentVault Function App CD with env

on:
  push:
    branches: [ main ]
    paths:
      - 'src/DocumentVault.Function/**'
      - '.github/workflows/function-app-cd.yml'
  workflow_dispatch:
    inputs:
      function_app_name:
        description: 'Select the function app to deploy'
        required: false
        default: 'func-docvaultghpctl27yftf6'
        type: choice
        options:
          - func-docvaultghpctl27yftf6
          - func-docvault3ftuued3odxl4
          - func-docvaultefgaj2t73so3m

permissions:
  id-token: write
  contents: read

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    env:
      DOTNET_VERSION: '8.0'
      AZURE_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
      AZURE_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
      SELECTED_APP: ${{ github.event.inputs.function_app_name || 'func-docvaultghpctl27yftf6' }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v3
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - run: |
          pushd './src/DocumentVault.Function'
          dotnet build --configuration Release
          dotnet publish --configuration Release --output ./output
          popd

      - name: Login (func-docvaultghpctl27yftf6)
        if: env.SELECTED_APP == 'func-docvaultghpctl27yftf6'
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID_FA_BRANCH }}
          tenant-id: ${{ env.AZURE_TENANT_ID }}
          subscription-id: ${{ env.AZURE_SUBSCRIPTION_ID }}

      - name: Deploy (func-docvaultghpctl27yftf6)
        if: env.SELECTED_APP == 'func-docvaultghpctl27yftf6'
        uses: Azure/functions-action@v1
        with:
          app-name: func-docvaultghpctl27yftf6
          package: './src/DocumentVault.Function/output'

      - name: Login (func-docvault3ftuued3odxl4)
        if: env.SELECTED_APP == 'func-docvault3ftuued3odxl4'
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID_ENV_SLOTS }}
          tenant-id: ${{ env.AZURE_TENANT_ID }}
          subscription-id: ${{ env.AZURE_SUBSCRIPTION_ID }}

      - name: Deploy (func-docvault3ftuued3odxl4)
        if: env.SELECTED_APP == 'func-docvault3ftuued3odxl4'
        uses: Azure/functions-action@v1
        with:
          app-name: func-docvault3ftuued3odxl4
          package: './src/DocumentVault.Function/output'

      - name: Login (func-docvaultefgaj2t73so3m)
        if: env.SELECTED_APP == 'func-docvaultefgaj2t73so3m'
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID_DEPLOYMENT_CENTER }}
          tenant-id: ${{ env.AZURE_TENANT_ID }}
          subscription-id: ${{ env.AZURE_SUBSCRIPTION_ID }}

      - name: Deploy (func-docvaultefgaj2t73so3m)
        if: env.SELECTED_APP == 'func-docvaultefgaj2t73so3m'
        uses: Azure/functions-action@v1
        with:
          app-name: func-docvaultefgaj2t73so3m
          package: './src/DocumentVault.Function/output'
